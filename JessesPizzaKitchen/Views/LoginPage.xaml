<?xml version="1.0" encoding="utf-8" ?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="JessesPizzaKitchen.Views.LoginPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Margin="20,40,20,20" BackgroundColor="{StaticResource backgroundColor}">
                <Label Margin="10" HorizontalTextAlignment="Center" Text="Login to use KDS for Jesse's Pizza" ></Label>
                <Entry x:Name="UserName"
                       Keyboard="Email"
                       Text="{Binding UserName, Mode= TwoWay}"
                   Placeholder="User Name">
                </Entry>

                <Entry x:Name="Password"
                   IsPassword="True"
                   Text="{Binding Password, Mode= TwoWay}"
                   Keyboard="Plain"
                       Placeholder="Password">
                </Entry>
                <Button Margin="0,50,0,0" x:Name="Login"
                    Style="{StaticResource ButtonStyle}"
                    Text="Login"
                    Clicked="Login_ClickedAsync">

                </Button>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>